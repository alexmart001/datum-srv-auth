server:
  port: 9000
spring:
  application:
    name: datum-srv-auth
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://${DB_HOST:localhost}:${DB_PORT:3306}/datum_db?useTimezone=true&serverTimezone=UTC
    username: ${DB_USERNAME:user_datum}
    password: ${DB_PASSWORD:C0ntrol3Xyz21}
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
    show-sql: false
    open-in-view: false

# Client OAuth2 usado pelo Postman (ou outra aplicação) para se autenticar
# junto ao Authorization Server. Identifica a APLICAÇÃO chamadora, não o
# usuário final - o usuário final é autenticado via username/password no
# corpo da requisição do grant "password" (ver OAuth2PasswordAuthenticationProvider).
datum:
  auth:
    issuer: http://${AUTH_HOST:localhost}:${AUTH_PORT:9000}
    client-id: postman-client
    client-secret: postman-secret
